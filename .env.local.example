# Copie este arquivo pra .env.local e cole sua chave da Tripo3D.
# O `vercel dev` carrega .env.local automaticamente.
# Em produção (Vercel deploy), configure a mesma variável no painel:
#   Settings → Environment Variables → TRIPO_API_KEY
TRIPO_API_KEY=cole_sua_chave_da_tripo_aqui
